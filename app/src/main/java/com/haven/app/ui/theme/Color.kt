package com.haven.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary ───────────────────────────────────────────────────────────────
val ForestGreen      = Color(0xFF2E7D32)   // Primary actions, headers, active states
val ForestGreenLight = Color(0xFF4CAF50)   // Secondary actions, highlights
val ForestGreenDark  = Color(0xFF1B5E20)   // Emphasis, contrast

// ─── Secondary ─────────────────────────────────────────────────────────────
val LeafGreen        = Color(0xFF81C784)   // Supporting elements, badges
val Mint             = Color(0xFFC8E6C9)   // Backgrounds, subtle fills

// ─── Accent ────────────────────────────────────────────────────────────────
val SoftYellow       = Color(0xFFFFD54F)   // Rewards, achievements, stars
val WarmOrange       = Color(0xFFFFB74D)   // Streaks, notifications
val SkyBlue          = Color(0xFF64B5F6)   // Info, calm states
val SoftPurple       = Color(0xFFCE93D8)   // Special events, milestones

// ─── Neutral ───────────────────────────────────────────────────────────────
val WarmWhite        = Color(0xFFFAFAF5)   // Primary background
val LightGray        = Color(0xFFF5F5F0)   // Card backgrounds, surfaces
val MediumGray       = Color(0xFF9E9E9E)   // Secondary text, icons
val DarkText         = Color(0xFF1B1B1B)   // Primary text

// ─── Semantic ──────────────────────────────────────────────────────────────
val SuccessGreen     = Color(0xFF4CAF50)
val WarningOrange    = Color(0xFFFFB74D)
val InfoBlue         = Color(0xFF64B5F6)
val ErrorCoral       = Color(0xFFE57373)   // Soft coral — not harsh red

// ─── Shadow / Overlay ──────────────────────────────────────────────────────
val WarmShadow       = Color(0x1A2E7D32)   // Warm-tinted card shadow
val OverlayDark      = Color(0x33000000)   // Bottom sheet backdrop